import { ComingSoon } from "@/components/coming-soon";

export default function PromptsPage() {
  return (
    <ComingSoon
      title="AI 프롬프트 라이브러리"
      step="Phase F 예정 — 프롬프트 카드 그리드 + 카테고리·해시태그 필터 + 클립보드 복사"
    />
  );
}
